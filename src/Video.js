export function Video(title, url, views, timestamp, thumbnail, channelName) {
    this.title = title;
    this.url = url;
    this.views = views;
    this.timestamp = timestamp;
    this.thumbnail = thumbnail;
    this.channelName = channelName;
}